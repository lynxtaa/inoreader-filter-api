<ul>
	<% for (const item of items) { %>
	<li class="py-1">
		<div class="d-flex">
			<%= item[type] %>
			<button
				type="button"
				class="btn btn-sm btn-outline-danger ml-auto"
				value="<%= `${type}-${item.id}` %>"
			>
				×
			</button>
		</div>
	</li>
	<% } %>
</ul>
